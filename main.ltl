// ltl-oci-manifest/main.ltl
//
// OCI image-manifest v1.0 reader / writer.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-oci-manifest v0.1.0")
    io::println("OCI image-manifest v1.0 reader / writer.")
    0
}
