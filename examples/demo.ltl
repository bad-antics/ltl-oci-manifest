// examples/demo.ltl — runnable demo for ltl_oci_manifest

import std::io
use ltl_oci_manifest as lib

fn main() {
    io::println("ltl_oci_manifest demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}